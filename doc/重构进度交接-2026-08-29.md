# 数据库重构进度交接（2026-08-29）

> 对应计划：doc/数据库重构计划.md（P1-P8）。本文档为会话交接用，任务板任务 #1-#9。

## 一、任务板状态

| # | 任务 | 状态 |
|---|------|------|
| 1 | 后端核心重构（启动顺序+getDB+meta引导+must_change_pwd迁移+默认管理员+删除保护） | ✅ 完成 |
| 2 | 发行物与废弃SQL清理（P2+P3） | ✅ 完成 |
| 3 | 前端：警告横幅+用户管理页（P7） | ✅ 完成 |
| 4 | 文档与CHANGELOG（P8） | ✅ 完成 |
| 5 | 验证：构建+测试矩阵+GUI走查 | 🔄 进行中（剩：最终实例复跑GUI走查） |
| 6 | 新需求-后端：站点配置可改接口+服务器登记上报接口（nrllink-78ham） | ⏳ 待办（被 #5 阻塞） |
| 7 | 新需求-前端：退出登录修复+站点设置页+服务器登记（nrllink-web-78ham） | ⏳ 待办（被 #6 阻塞） |
| 8 | 新需求-前端：全站响应式重构（nrllink-web-78ham） | ⏳ 待办 |
| 9 | 新需求-验证：nrllink-web-78ham 功能走查+多端适配 | ⏳ 待办（被 #6/#7/#8 阻塞） |

## 二、已确认的用户决策

1. must_change_pwd 用独立列（已完成）；
2. db/sqlite.sql、db/update.sql 直接删除（已完成）；
3. 提醒仅常驻横幅，无弹窗（已完成）；
4. 默认管理员全功能+提醒（已完成）；
5. （新需求）备案栏：从标题到页脚全部文案可在后台在线修改；
6. （新需求）内页不加页脚，仅登录页保留；
7. （新需求）"上报"含义＝登记我的服务器（进平台服务器列表，需后端接口支持）；
8. （新需求）UI 全站响应式重构；
9. Go 已由用户手动安装到 D:\Go（勿再自动安装，勿装 C 盘）。

## 三、已完成的代码改动（工作区未提交，勿丢失）

### 后端（nrllink-78ham）
- main.go：启动序列改为 getDB → execDDL（无条件）→ updatedb → ensureBootstrap。
- config.go：getDB 增强（NRL_DBFILE 环境变量、MkdirAll、首启提示、`_busy_timeout=5000`、PRAGMA integrity_check 损坏即退出、连接池上限 1）；新增 `Bootstrap.DefaultAdminCallsign` 配置（缺省 NOCALL）；updatedb 末尾追加迁移：must_change_pwd 加列 → routes='MUST_CHANGE_PWD' 搬运、坏库自愈补列（mdcid/dmrid/priority 变体文本）、rf_type 补列与 NULL 清理。**纪律：既有迁移语句文本一字未动，只追加。**
- init.go：execDDL 补 mdcid/dmrid/must_change_pwd/priority/rf_type/DEFAULT 列，新增 meta 表；getMeta/setMeta；isBootstrapped 改读 meta（存量有 admin 只补写不 seed）；seedAdmin 返回 bool、呼号可配置、冲突换随机占位、失败不写引导标记（下次重试）、写 meta.default_admin_id、操作日志；clearMustChangePwd 改新列；删除 MustChangePwdFlag。
- usersDB.go：userinfo 加 MustChangePwd 字段；getuser/getuserByID 补列（列序已评审核对）；deleteUser 返回 error 并清理 mdcidmap/dmridmap。
- users.go：登录与 /user/info 返回 must_change_pwd + default_admin；httpUpdateUserPassword 以 token 身份为准（修复强改密失效）；httpDeleteUser 服务端查库+仅禁删最后一个管理员+删默认管理员清 meta；httpUpdateUser 拦截降级/禁用最后一个管理员（条件 `stb.Status != 1`，已修旁路）；5 个列表查询（selectuser/getOperatorLog/selectrelay/selectReguser/getwxmsglist）已透传 args（评审问题1，已修）。
- http.go：补 /api/v1 别名：/user/password、/user/create、/user/delete。
- 编译修复（验证时发现的历史遗留编译错误）：globelconn 统一、queryToWhere 4 返回值、tools.go 返回值、codec2.go struct_CODEC2、udphub.go 重复 case 12（COM 透传已被 Codec2 700C 取代，forwardCOM 加注释、CHANGELOG 已记）。
- 发行物：udphub.sqlite3 已 git rm + 删盘；.gitignore 加 *.sqlite3*；start.sh 去掉复制自带库；删除 db/sqlite.sql、db/update.sql；Readme 目录树已清理。
- 文档：Readme.md 部署章节（自动建库、docker logs 取密码、默认账号五步指引、NRL_DBFILE、DBfile 建议放 /nrllink/data、坏库策略）；CHANGELOG 已补（含 must_change_pwd"从失效修复为真实生效"行为变化说明）。

### 前端（工作区 www/，Vue3+Naive UI，显式导入组件）
- stores/user.ts：User 接口加 must_change_pwd/default_admin；changePassword 带 id；新增 fetchUsers/createUser/deleteUser（users 列表用 POST '{}'）。
- Layout.vue：常驻黄色警告条（n-alert warning，不可关闭，条件 default_admin===true）；"用户管理"菜单（仅 admin）。
- UserList.vue（新建）：列表+新建（角色 admin/ham/view）+删除（popconfirm，删自己则登出跳登录页）；默认账号时"新建用户"按钮高亮。
- router.ts：/users 路由；ForceChangePwd.vue：文案改"首次登录请修改密码"+补表单校验。
- 走查后修复：App.vue 补 NConfigProvider/NMessageProvider/NNotificationProvider（此前缺失导致主题失效+用户管理页空白）；ForceChangePwd.vue 补 NForm 等导入（此前页面无输入框）；Settings.vue 修正 n-description→n-descriptions 并补导入。`npm run build` 通过。

## 四、验证结果

- 编译：go1.27.0（D:\Go）+ cgo 依赖（D:\mingw64 的 opus/opusfile/codec2/libogg，环境变量见下），`go vet`/`go build` 通过。
- 运行时矩阵（临时目录实测）：首启自动建库✅、二次启动无重复✅、改密闭环✅、最后管理员删除被拒✅、损坏库报错退出且文件不动✅。
- 评审（3 个重要问题）：已全部修复并复验（列表过滤查询、status!=1 旁路、seed 失败不写引导标记）。
- GUI 首走查：后端全部正常；前端 3 个导入缺陷已修复并重新构建。
- **剩余（任务#5）**：用修复后的二进制+新 dist 重启实例，复跑 GUI 走查（强改密页表单、/users 页面、删除流程）。

## 五、环境备忘

- Go：D:\Go\bin（当前会话需 `$env:PATH += ";D:\Go\bin"`）；cgo 需 `$env:CGO_CFLAGS="-ID:/mingw64/include"; $env:CGO_LDFLAGS="-LD:/mingw64/lib"; $env:PKG_CONFIG_PATH="D:/mingw64/lib/pkgconfig"; $env:GOPROXY="goproxy.cn,direct"`，且 D:\mingw64\bin 需进 PATH。
- 程序启动还需要 `udphub.ipdb`（IPfile），可复制 D:\udphub-test\udphub.ipdb；最小测试配置参照 D:\udphub-test\udphub.yaml。
- 二进制：udphub-test.exe（旧）、udphub-test2.exe（含评审修复，最新）。测试目录：D:\udphub-test、D:\udphub-gui（旧实例 18080）、D:\udphub-verify。
- 前端构建：cd www; npm run build（依赖已装）。

## 六、剩余工作（换软件后从这里继续）

1. **任务#5 收尾**：杀掉旧 18080 进程 → 清空/新建 D:\udphub-final → 用 udphub-test2.exe + 新 dist（Web.Path 指向 www\dist）+ 18080 启动 → 捕获随机密码 → 浏览器走查：登录→强改密页应有表单→改密→警告条→/users 列表正常→新建管理员→删除默认账号→警告消失→最后管理员保护。全部通过后任务 #5 完成。
2. **任务#6**（后端，当前工作区）：站点配置可改——标题/备案号/技术支持/版权等持久化（建议 meta 表或新表）+ /platform/info 返回最新 + POST /platform/update（admin 鉴权）；"登记我的服务器"——让普通登录用户可上报自己的服务器到服务器列表（评估放开 /server/create 或新增专用接口，记录归属）。
3. **任务#7**（D:\amnssb\Documents\78ham\nrllink-web-78ham，Vue3+Element Plus，独立项目）：①退出登录修复：src/store/modules/user.js 的 logout 改 try/finally 保证 removeToken 必执行 + src/layout/components/Navbar.vue logout 加 catch 后跳转 /login（根因：后端 50008/网络失败使 Promise.reject 中断流程）；②新增站点设置页（调 /platform/update）+ 登录页消费最新配置渲染标题与页脚（仅登录页）；③服务器登记入口表单（调任务#6 接口）。
4. **任务#8**（同项目）：全站响应式重构——登录页绝对定位/固定高度裁剪（login/index.vue 376-380/441-455/1479）、Logo 240px 固定宽、管理页 el-table 固定列宽横向溢出（如 setup/server.vue 约 2200px）、Navbar float 布局、固定 px 字号、Google Fonts 外链本地化、dashboard min-height 720px。
5. **任务#9**：构建+浏览器多端走查（桌面/窄窗口/移动视口）。
6. 全部完成后：清理测试进程与临时目录、删工作区编译产物（用户确认后再删）、三个独立 CodeReview（完整性/正确性/影响面）、交付总结。**勿擅自 git commit。**

## 七、独立前端项目调研要点（Eric 报告摘要，任务#6-#9 依据）

- 技术栈：vue-element-admin 改造，Vue3.5+Vite7+Pinia+Element Plus，hash 路由；开发代理 /dev-api → https://js.nrlptt.com/，生产 VITE_BASE_API='/' 同源自托管。
- 页脚现状：仅登录页有（login/index.vue 296-304），备案号来自 GET /platform/info（后端取 udphub.yaml 的 conf.Web.ICP），技术支持/版权硬编码；后端无任何写接口。
- 平台服务器列表：/platform/list 读 YAML 静态配置；DB 服务器登记：/server/*（admin 专属，setup/server.vue）。
- i18n 已预留 platformIcp 词条未使用；platforminfo 缺前端在读的 cs_qr_url 字段。
- 发现后端缺陷：首页 CMS 11 个 handler（homepage.go）未在 http.go 注册路由，前端公告请求 404（已静默容错）——任务#6 可顺带评估是否注册。
